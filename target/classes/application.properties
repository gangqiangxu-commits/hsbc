# Application properties for hello-springboot
server.port=8080

# Local MySQL datasource (adjust username/password as needed)
spring.datasource.url=jdbc:mysql://localhost:3306/hsbc_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=ganugxu612
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Use Flyway for DB migrations; enable automatic Flyway startup
spring.sql.init.mode=never
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# MyBatis: map snake_case columns to camelCase properties
mybatis.configuration.map-underscore-to-camel-case=true